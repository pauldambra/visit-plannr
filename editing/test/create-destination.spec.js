
describe('when creating a destination', function () {
  it('writes an event to a new stream')
  it('does not create duplicate destinations')
  it('rejects destinations with no geolocation')
  it('rejects destinations with no name')
})
