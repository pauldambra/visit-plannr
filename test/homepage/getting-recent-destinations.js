
describe('reading the most recent destinations from dynamodb', function () {
  it('can find the ids of the 5 most recent terminal events')
  it('can load the stream for each of the 5 most recent')
})
